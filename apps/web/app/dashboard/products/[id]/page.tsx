import { notFound, redirect } from "next/navigation";
import { updateProduct } from "@/app/actions/products";
import { createClient } from "@/lib/supabase/server";

export default async function ManageProductPage({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){
  const [{id},query]=await Promise.all([params,searchParams]);const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const {data:product}=await supabase.from("products").select("*").eq("id",id).eq("owner_id",user.id).maybeSingle();if(!product)notFound();return <div className="page-shell narrow"><span className="eyebrow">Maker dashboard</span><h1>Manage product</h1><p className="muted">Changes are sent back to the review queue.</p>{query.error&&<p className="form-message">{query.error}</p>}<form action={updateProduct} className="panel form"><input type="hidden" name="productId" value={product.id}/><div className="form-row"><div className="field"><label>Name</label><input name="name" defaultValue={product.name} required/></div><div className="field"><label>Website URL</label><input name="url" type="url" defaultValue={product.url} required/></div></div><div className="field"><label>Tagline</label><input name="tagline" maxLength={80} defaultValue={product.tagline} required/></div><div className="field"><label>Description</label><textarea name="description" maxLength={2000} defaultValue={product.description} required/></div><div className="form-row"><div className="field"><label>Category</label><input name="category" defaultValue={product.category} required/></div><div className="field"><label>Pricing</label><select name="pricingType" defaultValue={product.pricing_type??"free"}><option value="free">Free</option><option value="freemium">Freemium</option><option value="paid">Paid</option><option value="contact">Contact sales</option></select></div></div><button className="button button-accent">Save and submit for review</button></form></div>;
}
