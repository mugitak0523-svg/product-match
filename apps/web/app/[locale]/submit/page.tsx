import { submitProduct } from "@/app/actions/products";

export default async function SubmitPage({ searchParams }: { searchParams: Promise<{error?:string}> }) {
  const {error}=await searchParams;
  return <div className="page-shell narrow"><span className="eyebrow">For makers</span><h1>Enter your product</h1><p className="muted">Submit a product you own or operate. Every entry is reviewed before it joins an Arena.</p>{error&&<p className="form-message">{error}</p>}<form action={submitProduct} className="panel form" style={{marginTop:32}} encType="multipart/form-data"><div className="form-row"><div className="field"><label>Product name *</label><input name="name" maxLength={80} required/></div><div className="field"><label>Website URL *</label><input name="url" type="url" placeholder="https://" required/></div></div><div className="field"><label>Tagline * <span className="muted">80 characters max</span></label><input name="tagline" maxLength={80} required/></div><div className="field"><label>Description *</label><textarea name="description" maxLength={2000} minLength={20} required/></div><div className="form-row"><div className="field"><label>Category *</label><select name="category" required><option value="">Select</option><option>Developer Tools</option><option>Productivity</option><option>Design</option><option>AI</option><option>Marketing</option><option>Finance</option><option>Other</option></select></div><div className="field"><label>Pricing</label><select name="pricingType"><option value="free">Free</option><option value="freemium">Freemium</option><option value="paid">Paid</option><option value="contact">Contact sales</option></select></div></div><div className="form-row"><div className="field"><label>Logo *</label><input name="logo" type="file" accept="image/png,image/jpeg,image/webp" required/></div><div className="field"><label>Primary screenshot *</label><input name="screenshot" type="file" accept="image/png,image/jpeg,image/webp" required/></div></div><button className="button button-accent">Submit for review</button></form></div>;
}
